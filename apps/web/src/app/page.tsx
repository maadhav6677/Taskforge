export default function HomePage() {
  return (
    <main>
      <h1>TaskForge</h1>
      <p>A modular task automation platform with asynchronous execution.</p>
      <p>
        API + worker runtime scaffold is in place. Core auth, tasks, queue, storage, and persistence
        phases are next.
      </p>
    </main>
  );
}
